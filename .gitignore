# --- Dependencies ---
node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.env
.env.*

# --- OS ---
.DS_Store
Thumbs.db
.directory

# --- Logs ---
*.log
logs/

# --- Build artifacts ---
dist/
build/
*.tsbuildinfo

# --- TWA generated resources (produced by scripts/build-twa.cjs) ---
twa/app/build/
twa/app/src/main/res/mipmap-*/
twa/app/src/main/res/mipmap-*-round/
twa/app/src/main/res/values/twa-config.xml
twa/.gradle/
twa/local.properties

# --- Test artifacts ---
test-results/
playwright-report/
coverage/
latency-results.json

# --- IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- Caching ---
CACHEDIR.TAG

# --- LLM planning / AI session notes ---
llm/
CLAUDE.md
.claude/

# --- Agent work files (per-session, not tracked) ---
# NOTE: AGENTS.md and state.md are TRACKED — do not gitignore them.
workplan.md
lessons.md

# --- Model snapshots from HuggingFace (generated) ---
models--*/
snapshots/

# --- ONNX weight exports (NEVER commit) ---
# The pwa/model/*.onnx files in the repo were shape-only stubs that
# referenced missing .onnx.data sidecar files; they would crash at
# inference time. Real weights are produced by export_bvra_onnx.py /
# export_dima806_onnx.py (which download PyTorch checkpoints from HF
# and torch.onnx.export locally), then committed to a release bundle
# via .github/workflows/release.yml. Devs running pnpm dev locally
# should run the export scripts to populate pwa/model/ before serving.
pwa/model/*.onnx
pwa/model/*.onnx.data
pwa/model/*.onnx.external-data

# --- Secrets & credentials ---
*.pem
*.key
*_token*
credentials*
secrets*

# Type checking
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- Banned tool cruft (do not re-add) ---
graphify-out/
.gitnexusignore
.gitnexus/

# --- Security baseline (do not remove) ---
token.json
token_*.json
*token*.json
*.pickle
client_secret*.json
credentials.json
*.db-shm
*.db-wal
*.sqlite
